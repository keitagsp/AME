const change = document.getElementById('btn1');
const rains = document.getElementById('rains');
const span = document.querySelectorAll('span');

 change.addEventListener('click', () => {
    //  document.getElementById('fine').classList.toggle('rains');
    //  rains.classList.remove(rains);s
     rains.removeChild();
 });